runConfig:
  cpu: 1 # Default is 1, value can be from 1 up to 8
  memoryMiB: 1024 # Example: 1024 MiB (1 GiB). Default is 512. Min is 512.
  maxInstances: 10 # Optional: default is 100
  minInstances: 0 # Optional: default is 0
  concurrency: 100 # Optional: default is 80